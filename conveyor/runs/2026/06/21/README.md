# 2026-06-21 Run

This envelope holds the historical report and visual-evidence material moved out of the root `reports/` bucket during the conveyor-first reorganization.

Contents:

- `reports/` for the historical slice reports and seed reports
- `sketches/` for run-level ASCII evidence
- `slice-007h-sej-projected-ascii-render-kernel/` for the bounded ASCII render kernel proof run
- `slice-008-handoff-conveyor-cli-proof/` for the explicit CLI-to-Gemini Flash handoff proof run
- `slice-008b-ndd-cli-self-binding/` for the CLI self-binding run envelope and receipt-backed self-sketch
- `slice-008d-conveyor-filesystem-shape-lock/` for the filesystem shape-lock reconciliation packet
- `trust/` for surface-signature policy and schema artifacts

This envelope is historical posture, not the active source of truth.
