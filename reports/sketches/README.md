# Reports Sketches

This folder holds run-level ASCII visual evidence for meaningful slices.

The current `slice-007m1-run-visual-evidence-standard.ascii` file is a manual visual seed / ungoverned visual draft until a pipeline materialization receipt exists.

Slice 007M.3 now declares the resolver-mediated materialization path for that sketch, with receipt target `evidence/visual/run-sketches/slice-007m1-run-visual-evidence-standard.materialization.receipt.v1.json`, but the sketch remains ungoverned until the declared receipt is materialized.

Slice 007M.3A corrects the posture so this bridge is not mistaken for deterministic visual projection.

Future governed sketches must be produced through declared visual authority, materialization, and evidence.

Current convention:

- The sketch must show what the run achieved.
- The sketch should name its governing policy and schema when those exist.
- The sketch must distinguish passed, blocked, deferred, and stale artifacts.
- The sketch must link back to the textual slice report and any receipts or explicitly note why receipts are deferred.
- The sketch must not invent product truth.
- The sketch starts as ASCII and can later be rendered through the `visualProjectionModel`.

Related slice report:

- `../slice-007m1-run-visual-evidence-standard.latest.md`
