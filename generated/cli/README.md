# Generated CLI

This folder is the projected doorway surface for the `ndd` command family.

The Node entrypoint should remain thin and generated. The behavior authority lives in the CLI story, canonical spec, SEJ contracts, and projection manifests.

Slice 008B also classifies the Python and C# targets here as pending thin facades until their projection receipts exist.
