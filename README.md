# Defracto: Narrative Execution Fabric

Defracto is a truth-escalation system for turning preserved narrative into governed story, canonical specification, projections, lineage evidence, and reports.

It starts with the smallest trusted chain:

```text
source narrative
-> story packet
-> canonical spec
-> projection manifest
-> markdown projection
-> lineage receipt
-> narrative graph report
```

## Founding Principle

Intent is sovereign.
Narrative is source truth.
Canonical spec is authority.
Code is projection.
Evidence is memory.

## Repository Shape

- `docs/` explains the doctrine and implementation posture.
- `contracts/` declares the minimum schema and policy shapes.
- `conveyor/` will define the lanes and gates once the seed proof is established.
- `examples/` holds the founding conversation and its projected artifacts.
- `tools/` will hold validators and reporters after their lineage is defined.
- `reports/` holds human-readable proof surfaces.
- `evidence/` stores receipts and validation outputs.

## Current Posture

The repo is accepted for architecture.

Slice 000, Slice 000.1, Slice 000.2, and the manual-seed Slice 001 artifact review have passed.

The current proof posture is manual-seed, bootstrap-contract, resolver-backed facade materialization, operational doorway materialization, bootstrap invocation evidence, bootstrap closure, generalized SEJ validation contract expansion, resolver evidence consolidation, UI sketch projection contract review, UI sketch SEJ render contract review, UI sketch resolver capability handshake, UI sketch materialization SEJ review, resolver-backed UI sketch materialization, UI sketch deterministic layout review, UI sketch target sketch definition review, target sketch equivalence invocation evidence, target sketch projection path reconciliation, target sketch canonical-path rematerialization, target sketch equivalence validation, Visual Projection Model Contract report alignment in Slice 007G.1, Visual Projection Model schema/policy artifacts in Slice 007G.2, Deterministic ASCII Render Kernel contract review in Slice 007H, Visual Narrative Source contract review in Slice 007L, Visual Source Role-Change Receipt contract review in Slice 007M, Run Visual Evidence Standard in Slice 007M.1, and Run Sketch Authoring Violation Disposition in Slice 007M.2.
`tools/validate-lineage.js`, `tools/validate-story-packet.js`, and `tools/render-narrative-graph.js` exist only as resolver-materialized generated doorways.
No hand-authored executable validator body, SEJ runtime, resolver execution code, UI projection, or automation script exists yet.
The canonical target sketch validation has passed for `examples/target-sketches/projections/founding-lineage-flow.ascii`; the older doorway file remains a stale artifact until a later cleanup slice classifies or removes it.
Slice 007G now exists as `reports/slice-007g-visual-projection-model-contract.latest.md`.
The current system proves deterministic writing, not yet deterministic visual projection.
The `visualProjectionModel` is the product center for future renderers, and future ASCII, SVG, HTML/CSS, DOCX, PDF, PPTX, Google Docs, Google Slides, and video outputs must consume it.
Slice 007G.2 now exists as `contracts/schemas/visual-projection-model.schema.v1.json`, `contracts/schemas/visual-surface.schema.v1.json`, `contracts/schemas/visual-region.schema.v1.json`, `contracts/schemas/visual-render-adapter.schema.v1.json`, and `contracts/policies/visual-projection-determinism.policy.v1.json`.
Slice 007H now exists as `contracts/schemas/ascii-render-kernel.schema.v1.json`, `contracts/schemas/ascii-render-result.schema.v1.json`, `contracts/operations/ascii-render-kernel.operation-requirements.v1.json`, and `contracts/policies/ascii-render-kernel-boundary.policy.v1.json`.
The kernel is a governed mechanism only: it consumes `visualProjectionModel`, emits `asciiRenderResult`, and does not own artifact writing or receipt emission.
Slice 007L now exists as `contracts/schemas/visual-narrative-source.schema.v1.json`, `stories/sources/visual-narrative-source.story.v1.json`, and `specs/canonical/sources/visual-narrative-source.canonical-spec.v1.json`.
The repo now recognizes visual narratives as source intent surfaces when their role is explicitly declared and evidenced.
Slice 007M now exists as `contracts/schemas/source-role-change-receipt.schema.v1.json` and `contracts/policies/source-surface-role-change.policy.v1.json`.
The repo now recognizes role change as a governed receipt boundary from projection output to source-intent surface.
Slice 007M.1 now exists as `contracts/schemas/run-visual-evidence.schema.v1.json`, `contracts/policies/run-visual-evidence-required.policy.v1.json`, `reports/sketches/README.md`, and `reports/sketches/slice-007m1-run-visual-evidence-standard.ascii`.
Every meaningful slice now has a visual trace requirement, starting as ASCII and later renderable through the `visualProjectionModel`.
Slice 007M.2 now exists as `reports/slice-007m2-run-sketch-authoring-violation-disposition.latest.md`.
The existing 007M.1 sketch is treated as a manual visual seed or ungoverned visual draft until a pipeline materialization receipt exists.
